BoxComponent = class("BoxComponent")

function BoxComponent:__init(length, height, linked, func)
	self.length = length
	self.height = height
	self.selected = false
	self.linked = linked
	self.func = funct
	self.string = string
	self.pic = pic
end